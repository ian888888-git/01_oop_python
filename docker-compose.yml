version: '3.8'

services:
  dataops-app:
    build: .
    container_name: etl-dataops
    volumes:
      - .:/app
    tty: true          # <-- TAMBAHKAN BARIS INI (Biar kontainer standby)
    command: tail -f /dev/null
    environment:
      - ENV=development